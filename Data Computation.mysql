二、数据运算
1.运算符的优先级：'!'最高，详细请参考MySQL从入门到精通113页。当运算符优先级相同时，从左往右运算。可以用（）来改变优先级。


2.算术运算符：+、-、*、/、以及求余运算%。


3、比较运算符：不等于('!='或<>)，REGEXP(正则表达式符号，用来匹配满足特定条件的字符串)，IN(或NOT IN)，BETWEEN AND，IS NULL(或IS NOT NULL)，LIKE(或NOT LIKE)，GREATEST( GREATEST(10, NULL, 20)=NULL ), LEAST(当比较多值中含有NULL时，结果也为NULL), 以及<=>(用于比较2个值是否相等，注意与Python中不同).


4、逻辑运算符：非（！或者 NOT），或（OR 或者 ||），与（AND 或者 &&），异或（XOR）


5、位运算符：位或（|），位与(&)，位异或(^)，位左移(<<)，位右移(>>)，位取反(~). 位运算符为先将数值转换成二进制格式，然后对二进制各位置的数字进行逻辑运算。


8、
